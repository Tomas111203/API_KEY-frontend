# Example Environment Configuration Template (Safe to commit to version control)
API_KEY=your_api_key_here

# Use host.docker.internal to reach backend services running natively on host machine
BACKEND_HOST=host.docker.internal
BACKEND_PORT=8000
